spring:
  application:
    name: Trading-Signal-Tracking-application

  datasource:
    url: jdbc:postgresql://localhost:5432/zuvomo
    username: postgres
    password: SuMiT080403
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

binance:
  api:
    base-url: https://api.binance.com

scheduler:
  signal-poll:
    fixed-rate-ms: 60000

server:
  port: 8080

logging:
  level:
    com.tradingsignal.tracker: INFO

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

app:
  security:
    jwt:
      secret: 9bc61049ce30f844ed9931ba6f492d22bd4975dc7ce38c+464c74224f10dfac3
      expiration-ms: 3600000